name: drug_interaction_env
version: "1.0.0"
description: "Drug Interaction Env is a production-grade OpenEnv microservice for evaluating LLM reasoning over clinically realistic drug-drug interactions. It provides dense safety-aware rewards across easy, medium, and hard medication review tasks so agents can be benchmarked and trained with reproducible episodes."
